import { collection, doc, getDocs, getDoc, setDoc, deleteDoc, query, orderBy, limit } from 'firebase/firestore';
import { getFirebase } from './firebase.js?v=3';

const SEED_RULES = [
    {
        type: 'general',
        title: 'Фракции',
        content: 'В секторе действуют три расы: Красные, Синие и Фиолетовые.\n\n'
            + 'Красные и Синие — две расы, каждая со своим укладом. '
            + 'У них разные взгляды на многие вещи, и это иногда приводит к разногласиям. '
            + 'Однако обе стороны остаются частью общего пространства.\n\n'
            + 'Фиолетовые занимают особое положение. '
            + 'Встречи представителей разных рас проходят на их территории — '
            + 'дрейфующем в космосе шатле. '
            + 'Фиолетовые следят за порядком и помогают сторонам договариваться.',
        order: 1
    },
    {
        type: 'general',
        title: 'Документы',
        content: 'Документы — это записи с ограниченным доступом. '
            + 'Каждый документ имеет номер и код доступа.\n\n'
            + 'Текст может быть написан на разных языках — '
            + 'у каждой расы своя письменность. '
            + 'При необходимости текст можно переключить на привычное отображение.\n\n'
            + 'Доступ к документу можно получить несколькими способами:\n'
            + '  — Через ответственное лицо\n'
            + '  — По специальной ссылке\n'
            + '  — По номеру документа и коду доступа\n\n'
            + 'Уровни доступа:\n'
            + '  — Чтение (просмотр содержимого)\n'
            + '  — Полный доступ (чтение + просмотр оригинала)',
        order: 2
    },
    {
        type: 'general',
        title: 'Архив страниц',
        content: 'Архив — это хранилище информации. '
            + 'Страницы в архиве разделены по темам.\n\n'
            + 'Есть два типа страниц:\n'
            + '  — Общие страницы со свободным контентом\n'
            + '  — Страницы фракций, где информация структурирована по разделам\n\n'
            + 'Страницы могут быть вложенными — внутри одной страницы могут находиться другие.',
        order: 3
    },
    {
        type: 'site',
        title: 'Роли и доступы',
        content: 'На сайте действуют три роли:\n\n'
            + '🔹 Игрок (player) — базовый уровень. Имеет доступ к общим правилам, архиву страниц, документам (которые ему открыты), профилю и списку фракции.\n\n'
            + '🔹 Игротехник (igrotech) — помощник мастера. Дополнительно видит правила сайта, может создавать и редактировать общие правила и правила сайта. Имеет доступ к админке (кроме раздела скрытых механик).\n\n'
            + '🔹 Мастер (master) — полный доступ. Видит все разделы правил, включая скрытые механики. Управляет тегами, страницами, документами, глобальным дизайном. Может редактировать и удалять любой контент.\n\n'
            + 'Роль назначается мастером через базу данных. Самостоятельно сменить роль нельзя.',
        order: 4
    },
    {
        type: 'site',
        title: 'Фракции и видимость контента',
        content: 'Каждому пользователю может быть назначена фракция: red, blue или purple.\n\n'
            + 'Фракция влияет на цветовое оформление сайта (тема подстраивается автоматически):\n'
            + '  — Красные — красная тема\n'
            + '  — Синие — синяя тема\n'
            + '  — Фиолетовые — фиолетовая тема\n'
            + '  — Без фракции — серая тема\n\n'
            + 'Также фракция определяет видимость контента:\n'
            + '  — Общие страницы (general) доступны всем\n'
            + '  — Фракционные страницы видны только соответствующей фракции\n'
            + '  — Фиолетовые видят контент всех фракций\n'
            + '  — Скрытые теги и специальные пометки обрабатываются отдельно\n\n'
            + 'Фракция назначается и меняется мастером.',
        order: 5
    },
    {
        type: 'site',
        title: 'Документы: создание и доступ',
        content: 'Документы — защищённые записи с уникальным номером и 8-значным кодом доступа.\n\n'
            + 'Кто может создавать:\n'
            + '  — Мастера и игротехники\n'
            + '  — При создании указывается фракция документа и уровень доступа\n\n'
            + 'Способы получения доступа:\n'
            + '  — Мастер выдаёт доступ вручную через интерфейс документа\n'
            + '  — По QR-коду (ссылка с кодом доступа) — печатается и распространяется физически\n'
            + '  — По номеру документа + 8-значный код (ввод на странице добавления)\n\n'
            + 'Уровни доступа:\n'
            + '  — Чтение — просмотр содержимого документа\n'
            + '  — Полный — просмотр содержимого и оригинала (с письменностью фракции)\n\n'
            + 'Пропаганда: документ может быть помечен как пропаганда. '
            + 'Для целевой фракции такой документ выглядит как обычный — маркировка скрыта. '
            + 'Истинная пометка видна только создателю и фиолетовым.\n\n'
            + 'Управление документами (редактирование, удаление, выдача доступа) — через интерфейс документа для мастеров и игротехников.',
        order: 6
    },
    {
        type: 'site',
        title: 'Архив: теги и видимость',
        content: 'Архив страниц использует систему тегов для организации и ограничения доступа.\n\n'
            + 'Типы тегов:\n'
            + '  — Фракционные (red, blue, purple) — определяют, какой фракции доступна страница\n'
            + '  — Уровневые (red_1, red_2, ..., purple_3) — уточняют уровень доступа внутри фракции\n'
            + '  — Скрытые — видны только владельцу и фиолетовым; сам факт существования тега невидим для других\n'
            + '  — Технические (субтеги) — служебные теги, доступные только мастерам\n\n'
            + 'Типы страниц:\n'
            + '  — general — свободный контент с изображениями\n'
            + '  — faction — матрица 3×3 для фракционного контента, ячейки открываются по составным тегам\n\n'
            + 'Страницы могут быть вложенными (родитель-потомок). Навигация осуществляется через дерево страниц в разделе «Архив».\n\n'
            + 'Управление тегами — в админке, раздел «Каталог грифов» (только для мастеров).',
        order: 7
    },
    {
        type: 'site',
        title: 'Профиль и заметки',
        content: 'В разделе «Досье» отображается профиль пользователя.\n\n'
            + 'Поля профиля:\n'
            + '  — Позывной (username) — уникальное имя, задаётся при регистрации\n'
            + '  — Фракция — назначается мастером\n'
            + '  — Мировоззрение — текст, заполняется пользователем\n'
            + '  — О себе — текст, заполняется пользователем\n'
            + '  — Теги доступа (accessTags) — видны всем, редактируются пользователем\n'
            + '  — Скрытые теги (hiddenTags) — видны только владельцу и фиолетовым\n'
            + '  — Технические субтеги (factionAccessTags) — только для мастеров\n\n'
            + 'Заметки об игроке:\n'
            + '  — Мастер и игротехники могут писать заметки в профиле любого пользователя\n'
            + '  — Заметки видны только мастерам и игротехникам\n'
            + '  — Игрок видит только свои заметки\n\n'
            + 'Редактирование профиля — через кнопку «Редактировать» на своей странице досье.',
        order: 8
    },
    {
        type: 'site',
        title: 'Навигация и интерфейс',
        content: 'Сайт представляет собой одностраничное приложение (SPA) с хеш-роутингом.\n\n'
            + 'Основные разделы:\n'
            + '  — Главная — приветственная страница с навигацией по модулям\n'
            + '  — Архив — дерево страниц и матрица фракций\n'
            + '  — Документы — список доступных документов\n'
            + '  — Правила — сводка правил с боковым меню\n'
            + '  — Фракция — список участников по фракциям и ролям\n'
            + '  — Досье — личный профиль\n'
            + '  — Мастерская (админка) — управление сайтом (только мастера и игротехники)\n\n'
            + 'Мобильная версия:\n'
            + '  — Навигация скрывается за гамбургер-меню\n'
            + '  — В разделе правил боковое меню открывается по кнопке «Содержание»\n\n'
            + 'Темы оформления:\n'
            + '  — Тема автоматически подстраивается под фракцию пользователя\n'
            + '  — Мастер может переключать глобальный дизайн (v1 / v2) в админке',
        order: 9
    },
    {
        type: 'site',
        title: 'Администрирование',
        content: 'Раздел «Мастерская» доступен мастерам и игротехникам.\n\n'
            + 'Возможности:\n'
            + '  — Переключение глобального дизайна (v1 — светлая тема, v2 — тёмная космическая)\n'
            + '  — Управление каталогом тегов (добавление/удаление фракционных и уровневых тегов)\n'
            + '  — Управление техническими субтегами (автоматически создаются из тегов фракционных страниц)\n'
            + '  — Управление архивом страниц (создание, редактирование, удаление)\n'
            + '  — Просмотр списка документов с читателями\n'
            + '  — Обновление правил по эталону (только мастера)\n\n'
            + 'Игротехники видят админку в ограниченном режиме — без раздела скрытых механик.',
        order: 10
    },
    {
        type: 'hidden',
        title: 'Скрытые механики',
        content: 'Раздел будет заполнен мастерами.',
        order: 5
    }
];

export async function getAllRules(typeFilter) {
    const { db } = getFirebase();
    const constraints = [orderBy('order')];
    if (typeFilter) {
        const { where } = await import('firebase/firestore');
        constraints.unshift(where('type', '==', typeFilter));
    }
    const snapshot = await getDocs(query(collection(db, 'rules'), ...constraints));
    const rules = [];
    snapshot.forEach(d => rules.push({ id: d.id, ...d.data() }));
    return rules;
}

export async function getRule(id) {
    const { db } = getFirebase();
    const snap = await getDoc(doc(db, 'rules', id));
    if (!snap.exists()) return null;
    return { id: snap.id, ...snap.data() };
}

export async function createRule(data) {
    const { db } = getFirebase();
    const ref = doc(collection(db, 'rules'));
    const payload = {
        type: data.type || 'general',
        title: data.title.trim(),
        content: data.content.trim(),
        order: data.order || 0,
        createdAt: new Date().toISOString(),
        updatedAt: new Date().toISOString()
    };
    await setDoc(ref, payload);
    return { id: ref.id, ...payload };
}

export async function updateRule(id, data) {
    const { db } = getFirebase();
    const updates = {};
    if (data.title !== undefined) updates.title = data.title.trim();
    if (data.content !== undefined) updates.content = data.content.trim();
    if (data.order !== undefined) updates.order = data.order;
    if (data.type !== undefined) updates.type = data.type;
    updates.updatedAt = new Date().toISOString();
    await setDoc(doc(db, 'rules', id), updates, { merge: true });
    return { id, ...updates };
}

export async function deleteRule(id) {
    const { db } = getFirebase();
    await deleteDoc(doc(db, 'rules', id));
}

export async function seedInitialRules() {
    const { db } = getFirebase();
    const snapshot = await getDocs(collection(db, 'rules'));
    const ids = [];
    snapshot.forEach(d => ids.push(d.id));

    for (const id of ids) {
        await deleteDoc(doc(db, 'rules', id));
    }

    for (const data of SEED_RULES) {
        const ref = doc(collection(db, 'rules'));
        await setDoc(ref, {
            ...data,
            createdAt: new Date().toISOString(),
            updatedAt: new Date().toISOString()
        });
    }
}
